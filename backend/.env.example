# Supabase database connection string (project: giumudtoeraxfckljqgc).
# Dashboard → Project Settings → Database → Connection string → URI (or use below).
# Replace [YOUR-PASSWORD] with your database password.
#
# If you get "getaddrinfo failed": use the Session pooler instead. In the dashboard go to
# Connect → Session mode and copy that URI (host will be aws-0-XX.pooler.supabase.com).
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.giumudtoeraxfckljqgc.supabase.co:5432/postgres
